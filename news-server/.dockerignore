# Docker 빌드 시 제외할 파일 및 폴더 목록

# 의존성 폴더 (컨테이너 내부에서 설치됨)
node_modules

# 로컬 빌드 결과물 (컨테이너 내부에서 빌드됨)
dist

# 환경변수 파일 (Docker-compose에서 주입함)
.env

# 로그 파일 (모든 위치)
npm-debug.log
logs
*.log

# Python 캐시
__pycache__
*.pyc
*.pyo